# Web API Test Utils

Test utilities for HTTP API endpoints. Provides `ErrorsEndpointHttpTestSpecification` (reusable tests for invalid access, bad content type, malformed JSON, invalid version), `ApiError` assertion helpers, and `TestContainersLocalPodWithDrivingAdapter` for container-based integration tests with web driving adapters.
