# Avro Schema Rules

Acme compliance rules for Avro schema definitions. Enforces uppercase schema names, mandatory `acme.` namespace prefix, and whitelisted alphabets for schema names (camelCase), namespace names (lowercase dot-separated), and field names (lowercase_underscore).
