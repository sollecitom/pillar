# Avro Serialization DDD

Avro serdes for DDD event types: `Event.Context`, `Event.Metadata`, `Event.Reference`, `HappeningType`, and a composite `EventCompositeAvroSerde`. Also provides `WithEventAvroSerdeSupport` for building event-specific Avro serdes with standard event structure.
