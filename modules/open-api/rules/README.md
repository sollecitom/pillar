# OpenAPI Rules

Acme compliance rules for OpenAPI specifications. Enforces lowercase-kebab path names, camelCase operation IDs, mandatory request body for POST/PUT/PATCH, forbidden request body for GET/DELETE, mandatory examples for JSON content types, schema compliance of examples, whitelisted OpenAPI versions (3.0.0, 3.1.0), and text field formatting rules.
