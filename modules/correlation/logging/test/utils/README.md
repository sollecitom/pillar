# Correlation Logging Test Utils

AssertK assertions for verifying that log output contains the expected invocation context. Provides `haveContextForkedFrom()` and `haveContext()` assertions that parse invocation contexts from log lines and compare them.
