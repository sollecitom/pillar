# Correlation Logging Utils

Bridges invocation context with structured logging. Provides `InvocationContext.toLoggingContext()` and `withLoggingContext()` using Acme's JSON serialization to include the full invocation context in log entries.
