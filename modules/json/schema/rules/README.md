# JSON Schema Rules

Acme compliance rules for JSON Schema definitions. Enforces lowercase-kebab field names, mandatory `additionalProperties: false`, disallows requiring undeclared properties, and disallows the `const` keyword.
