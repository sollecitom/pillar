# JSON Serialization DDD

JSON serdes for DDD event types: `Event.Context`, `Event.Metadata`, `Event.Reference`, and `HappeningType`. Used to serialize domain events with their full context (invocation, parent/originating references).
