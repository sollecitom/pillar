# HTTP API Conventions

Defines the Acme company-wide `HttpApiDefinition` with standard HTTP header names prefixed with `acme-`. Provides `HttpApiDefinition.companyWide` as the default API definition for all Acme services.
