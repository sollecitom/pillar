# Acme Business Domain

Core Acme business objects. Defines the `Acme` singleton (tenant, roles, message property names, HTTP API definition, default locale) and `AcmeRoles` (customer, expert, admin). Also contains the legacy `Example` object (marked for removal).
