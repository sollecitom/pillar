# Encryption Messaging Serialization Avro

Avro serdes for encryption metadata types: `CryptographicKey.Metadata` and `CtrEncryptionMetadata`. Used when serializing encrypted message payloads that need to carry key algorithm, format, and hash information.
