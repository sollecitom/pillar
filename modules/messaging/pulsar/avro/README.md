# Messaging Pulsar Avro

Convenience extensions on `PulsarClient` to create `MessageConsumer` and `MessageProducer` instances from a `TopicAndSerde`, simplifying Pulsar integration for Avro-serialized events.
