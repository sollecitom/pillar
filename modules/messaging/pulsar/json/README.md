# Messaging Pulsar JSON

Convenience extensions on `PulsarClient` to create `MessageConsumer` and `MessageProducer` instances from a `TopicAndJsonSerde`, simplifying Pulsar integration for JSON-serialized events.
