# Messaging Test Utils

Test utilities for event processing. Provides `processAndWaitUntilAllAcked()` which creates an event processor from a list of spy messages, starts processing, and waits until all messages are acknowledged.
