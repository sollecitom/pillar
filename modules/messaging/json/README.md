# Messaging JSON

Provides `TopicAndJsonSerde`, which bundles a messaging `Topic` with a `JsonSerde.SchemaAware` and implements `MessageConverter`. Used to define event topics with their JSON serialization format.
