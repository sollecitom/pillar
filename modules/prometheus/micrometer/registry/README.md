# Prometheus Micrometer Registry

Factory for creating a pre-configured `PrometheusMeterRegistry` with standard JVM and system meter binders (memory, GC, threads, disk space, CPU, uptime). Requires `MonitoringConventions` in context.
