# Service Logging

Standard service lifecycle log messages. Provides `logServiceStarted()` and `logServiceStopped()` extension functions on `Loggable`, with conventional log messages (`"--Service started--"` and `"--Service stopped--"`). Requires `LoggingConventions` in context.
