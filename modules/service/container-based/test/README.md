# Service Container-Based Test

Testcontainers utility for waiting until a containerized service has started. Provides `waitingForServiceStarted()` which waits for the standard service-started log message in the container output.
