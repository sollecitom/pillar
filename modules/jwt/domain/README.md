# JWT Domain

Acme-specific JWT domain types and claim parsing. Defines `User`, `Organization`, `Access`, `Authentication`, `Token`, `Session`, `Issuer`, `TargetApplication`, and `OpenIdConnectParams`. The `AcmeJwtScheme` object handles serialization and deserialization of Keycloak-based JWT claims to and from these types.
